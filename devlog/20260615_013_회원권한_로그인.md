# 20260615_013 — 회원/권한 (운영진 로그인)

## 추가
- Django 기본 인증 사용. `accounts/login/`(LoginView), `accounts/logout/`(LogoutView, POST).
- `templates/registration/login.html` — Bootstrap 로그인 폼(운영진 전용 안내).
- settings: `LOGIN_URL`, `LOGIN_REDIRECT_URL`, `LOGOUT_REDIRECT_URL`(홈).
- base.html 네비 분기:
  - 익명 → "로그인"
  - 로그인 → "로그아웃(아이디)" (POST 폼), 운영진(is_staff)에게만 "관리"(/admin/) 노출.

## 비고
- 공개 페이지는 읽기 전용으로 모두 공개. 데이터 입력은 기존대로 admin(운영진) 전용.
  프론트 로그인은 운영진 진입점/방문자 구분 용도.

## 테스트
- 로그인 페이지 200, 익명/운영진 네비 분기, 로그아웃 리다이렉트. 전체 18 tests OK.
