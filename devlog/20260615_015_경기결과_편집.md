# 20260615_015 — 경기 결과 편집 (운영진)

## 목표
- 운영진이 **사이트 안에서** 경기 결과(최종 스코어 + 득점자·도움·득점 시간)를 편집.

## 추가 (사이트 내 편집)
- `apps/matches/forms.py` — `MatchResultForm`(스코어·상태·비고) + `MatchEventFormSet`
  (`inlineformset_factory(Match, MatchEvent)`, 빈 줄 3 + 삭제).
- `apps/matches/views.py` — `match_edit`(`@user_passes_test(is_staff)`): 익명/비운영진은 로그인으로.
  저장 시 `messages.success` 후 상세로 리다이렉트.
- `apps/matches/urls.py` — `matches/<pk>/edit/` (name=`edit`).
- `templates/matches/match_edit.html` — 모바일 친화 카드 폼 + 이벤트 표,
  바닐라 JS "행 추가"(`empty_form` 복제, 빌드툴 없음).
- `templates/matches/match_detail.html` — 운영진 전용 **"결과 편집"** 버튼.

## admin 보강 (apps/matches/admin.py)
- Match 폼을 **경기 정보 / 결과 / 비고** 필드셋으로 그룹화 + 입력 안내.
- 이벤트 인라인 컬럼·정렬 정비(이벤트유형·팀·선수·분·설명), 목록에 스코어·승무패 컬럼.

## 테스트
- 익명 edit → 로그인 리다이렉트(`?next=`), 운영진 GET 200, POST 저장(스코어·이벤트 반영) 후
  상세로 302. nginx 경유 동일 확인.
