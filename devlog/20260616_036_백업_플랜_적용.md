# 20260616_036 — 백업 플랜 적용 (fsis2026 → FC Sky)

## 한 일

`../fsis2026`의 3계층 백업을 FC Sky 규모로 단순화해 옮기고, 두 호스트에 실제 설치·가동했다.

- **운영 hourly** (dolfinid, honestjung@34.64.158.160): `scripts/backup_db.py`
  - SQLite online backup(`fcsky`) + `/etc/nginx/sites-available` tar(`dolfinid_nginx`)
  - 최근 12시간 rolling, 디스크 가드 `MIN_FREE_GB=2`
  - 배치: `/srv/FcSky/scripts/backup_db.py`, cron `0 * * * *`
- **백업 daily pull** (m710q): `scripts/backup-fcsky.sh`
  - DB 계층 보존(30일 매일→월초→12월 영구), media link-dest 스냅샷(월간 full + 일간),
    .env·NAS·dev_data는 있을 때만(graceful)
  - 배치: `/home/jikhanjung/scripts/backup-fcsky.sh`, cron `0 4 * * *`
- **매뉴얼**: `docs/operation_manual/backup.md` (구조·설치·복원·점검)

## fsis2026 대비 단순화

- ghdb 트랙·`data/` 분류 JSON 트랙 제거(FC Sky엔 해당 없음).
- uploads(14GB) → media(3.8MB)로 규모 작음. 구조(link-dest 체인)는 유지.
- `MIN_FREE_GB` 5→2 (소규모).

## 적용 검증 (실제 실행)

- dolfinid: `backup_db.py` 1회 실행 → `fcsky_20260616_08.sqlite3`(0.3MB) +
  `dolfinid_nginx_*.tar.gz` 생성, exit 0. crontab에 FcSky 항목 추가(기존 fsis 항목 보존).
- m710q: `backup-fcsky.sh --full-snapshot` → DB·media·nginx tar pull, `202606_full` 생성,
  exit 0. crontab `0 4 * * *` 추가.
- 두 호스트 cron 모두 라이브.

## 메모

- dolfinid `/srv/FcSky` 소유는 ubuntu:ubuntu, honestjung는 ubuntu 그룹이라 backup 디렉토리
  쓰기 가능. DB(664)·nginx 읽기 가능.
- `.env`는 `/srv/FcSky`에 없음 → 백업은 best-effort(WARN skip). 위치 확인되면 추가.
- NAS·dev_data 디렉토리는 현재 m710q에 없어 skip. 만들면 자동 포함.
- 이미지 빌드에서 `scripts/`·`docs/` 제외(dockerignore).
- ⚠️ 운영 컨테이너 가동 중 호스트에서 직접 `manage.py` 대량 쓰기·migrate 금지(DB 손상 위험).
