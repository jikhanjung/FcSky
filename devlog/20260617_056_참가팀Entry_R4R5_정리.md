# 20260617_056 — 참가팀(Entry) 개편 R4/R5 정리

> 선행: [055 R1-R3](20260617_055_참가팀Entry_전면개편_R1R3.md).

## 한 일

사용자 결정으로 `seed`·`load_seocho_knockout` 시드 명령이 더 이상 불필요 → 삭제하며 R4/R5 마무리.

- **명령 삭제**: `apps/teams/management/commands/seed.py`,
  `apps/matches/management/commands/load_seocho_knockout.py` (구 모델 의존, 불필요).
- **entrypoint**: `DJANGO_SEED` 시드 블록 제거. compose 2종의 `DJANGO_SEED` env·주석 제거.
  CLAUDE.md 의 `seed` 명령 줄 제거.
- **테스트 갱신**(테넌트 URL + seed 비의존):
  - `teams/tests.py`: seed 기반 스모크 → `/fcsky/` 경로 + Club 픽스처, 루트 302·미지정 클럽 404 검증.
  - `notices`·`gallery` 테스트: `/fcsky/` 프리픽스 + Club 픽스처.
  - `matches`·`competitions` 테스트는 055에서 entry 모델로 갱신 완료.
- **버그 수정**: `opponent_match_edit.html` 의 `om.home`/`om.away`·`get_age_group_display`
  → `home_entry.name`/`away_entry.name`·`division.label` (참가팀 개편 후 유일한 실템플릿 버그).

## 검증

- `manage.py check` 무이슈, **전체 18개 테스트 통과**.
- `seed`·`load_seocho_knockout` → "Unknown command"(정상 삭제).

## 상태

참가팀(Entry) 전면 개편 **완료**. 운영 앱 경로 전부 동작, 테스트 그린.
다음: SaaS **B2(쿼리 스코핑)** — 클럽별 데이터 필터 + 쓰기 시 club 주입.
